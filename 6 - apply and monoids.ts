// apply.sequenceT
// E.getApplicativeValidation
// A.getSemigroup

// const sequence = apply.sequenceT(E.getApplicativeValidation(A.getSemigroup<ValidationErrorReason>()));

//  const validateA = <T>(data: T): Either<NonEmptyArray<ValidationErrorReason>, T> =>
//   pipe(
//     data,
//     E.fromPredicate(
//       () => isDefined(data).
//       () => ['empty']
//     )
//   );

// pipe(
//   sequence(
//      validateA,
//      validateB,
//      validateC,
//      validateD,
//      validateE,
//   )
// )

// apply.sequenceS
